{
  "description": "Nudge the user to run /mcp-steroid:setup when devrig is not yet installed.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/check-devrig"
          }
        ]
      }
    ]
  }
}
