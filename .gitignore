# Gradle
.gradle/
.gradle*/
.kotlin
*.kotlin_module
build/

.claude/scheduled_tasks.lock
.claude/worktrees

.worktrees/

# IntelliJ IDEA
.idea/
!.idea/dictionaries

out/
.intellijPlatform/

.vscode

.playwright-mcp

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Integration test temporary files
integration-test/tmp/
integration-test/*.tmp

# DPAIA run output directories (agent stdout/stderr/stream logs)
runs/run_*/

# run-agent.sh handoff dirs (codex / claude / gemini per-task transcripts)
.run-agent-*/

# NPX package artifacts
npx/node_modules/
npx/dist/
npx/dist-test/
npx/.npm/
npx/.cache/
npx/*.log

# Installer scripts are GENERATED by :site-gen (website build + integration tests) — never committed.
# (There is no longer an intermediate jdk-coordinates.json: :site-gen:generateInstaller inspects the
# downloaded JDK archives ad-hoc and bakes everything straight into the scripts.)
/website/static/install.sh
/website/static/install.ps1
